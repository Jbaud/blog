<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :name, 'Nom' %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :body, 'Texte' %><br>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit "Soumettre" %>
<% end %>